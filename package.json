{
  "name": "ekparivar",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --dir frontend dev\" \"pnpm --dir backend dev\"",
    "frontend": "pnpm --dir frontend dev",
    "backend": "pnpm --dir backend dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}